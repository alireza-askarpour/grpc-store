syntax = "proto3";

package categoryPackage;

message CreateCategory {
    string name = 1;
    string value = 2;
    string parent = 3;
}

message Empty {}

service CategoryService {
    rpc createCategory (CreateCategory) returns (Empty);
}
